# mihomo-cli 软件设计说明

> 一个面向 Linux 服务器的纯命令行 Clash 代理工具 —— mihomo 内核的 CLI 外壳
>
> 版本: v0.1 (草案) / 日期: 2026-09-20

---

## 1. 项目背景与目标

### 1.1 背景

GitHub 上的 Clash 生态客户端（Clash Verge Rev、FlClash、ClashN 等）几乎全是 GUI 软件，其底层都调用 **mihomo**（原 Clash.Meta）内核。而 Linux 服务器没有图形环境，GUI 客户端无法使用；手动管理 mihomo（下载内核、写 systemd 单元、更新订阅、切换节点）又非常繁琐。

### 1.2 目标

`mihomo-cli` 是一个 **单二进制、零依赖、纯 CLI** 的 mihomo 内核管理外壳，提供：

- 内核的自动下载、升级、回滚
- 订阅的下载、定时更新、多订阅管理
- 代理的启停（托管为 systemd 服务，无 GUI）
- 节点/代理组的查看与切换
- 连接、流量、日志的实时查看
- 一键配置系统代理环境变量（`http_proxy` 等）
- 安装/卸载一体化

### 1.3 非目标

- 不做 GUI / TUI（未来可选加一个简单的交互式菜单，但不是核心）
- 不重新实现代理内核，只做 mihomo 的包装与编排
- 不支持 Windows / macOS（设计上留有余地，但优先保证 Linux 体验）

---

## 2. 总体架构

```
┌──────────────────────────────────────────────────────┐
│                    用户 (SSH 终端)                    │
└───────────────────────┬──────────────────────────────┘
                        │ mihomo <subcommand> [flags]
┌───────────────────────▼──────────────────────────────┐
│                   mihomo-cli (单二进制)               │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌────────────────────┐  │
│  │ CLI 层    │  │ 核心服务  │  │  RESTful API 客户端 │  │
│  │ cobra     │  │ 编排层   │  │  (external-controller)│
│  └────┬─────┘  └────┬─────┘  └─────────┬──────────┘  │
│       │              │                  │             │
│  ┌────▼──────────────▼──────────────────▼──────────┐  │
│  │                基础库层                           │  │
│  │  内核管理 │ 订阅管理 │ 配置生成 │ systemd │ 自更新  │  │
│  └─────────────────────────────────────────────────┘  │
└───────┬──────────────┬───────────────────┬────────────┘
        │ 文件          │ HTTP              │ 进程托管
┌───────▼──────┐ ┌─────▼─────┐   ┌─────────▼─────────┐
│ ~/.config/   │ │ 订阅 URL   │   │ mihomo 内核 (子进程)│
│ mihomo-cli/  │ │ GitHub API│   │ + systemd 服务     │
└──────────────┘ └───────────┘   └───────────────────┘
```

### 2.1 分层说明

| 层 | 职责 |
|---|---|
| **CLI 层** | 基于 spf13/cobra 的子命令树，参数解析、帮助信息、shell 补全 |
| **编排层** | 业务逻辑：安装流程、订阅刷新流程、服务生命周期 |
| **API 客户端** | 通过 mihomo 的 `external-controller` RESTful API 查询/切换节点、查连接、查流量 |
| **内核管理** | 从 GitHub Releases 下载/校验/升级 mihomo 二进制，按 arch 自动选择资产 |
| **订阅管理** | 下载订阅（clash yaml / base64）、自动更新、多配置切换 |
| **配置生成** | 将订阅配置与用户覆盖项（端口、API secret、TUN 等）合并生成最终 config.yaml |
| **systemd** | 生成/安装 `mihomo-cli.service`，以服务方式常驻（也支持前台调试模式） |

### 2.2 关键设计决策

1. **cli 不 fork 内核源码**，通过子进程 + RESTful API 控制，内核可独立升级。
2. **配置三层合并**：`订阅原始配置` ← `用户覆盖(overrides.yaml)` ← `cli 生成的运行时项(端口/secret/日志)`。订阅更新不会冲掉用户自定义。
3. **单二进制**：Go 编写，静态编译，scp 到任何 Linux 服务器即可用。
4. **systemd 托管为默认**，服务器重启自动拉起；`mihomo run` 前台模式用于调试。

---

## 3. 工作模式

### 3.1 服务模式（默认）

```
mihomo-cli start  →  systemctl start mihomo-cli  →  mihomo 内核以 root(NOPASSWD) 或专用用户运行
```

- 内核配置文件由 cli 生成并注入 `external-controller: 127.0.0.1:9090` + 随机 secret
- cli 的所有查询/切换操作都走该 API

### 3.2 前台模式（调试）

`mihomo run` 直接在当前终端启动内核，日志输出到 stdout，Ctrl-C 退出。适合排查节点问题。

### 3.3 环境变量模式（按需开代理）

`mihomo env` 输出（或 `eval $(mihomo-cli proxy on)` 注入）：

```bash
export http_proxy=http://127.0.0.1:7890
export https_proxy=http://127.0.0.1:7890
export all_proxy=socks5://127.0.0.1:7891
export no_proxy=localhost,127.0.0.1
```

---

## 4. 目录规划（系统级, v0.3 起）

服务端(内核)/客户端(CLI)/配置全机共享; 多用户共用同一套代理。管理命令需 sudo, 查询命令全用户可用。
旧版 `~/.config/mihomo-cli` 由 root 运行时自动迁移。

```
/etc/mihomo-cli/
├── config.toml              # cli 自身设置 (644)
├── overrides.yaml           # 用户对订阅配置的深度覆盖
├── profiles/
│   └── <name>.yaml          # 各订阅的原始下载文件
├── runtime/
│   └── config.yaml          # 合并后给内核的实际配置(自动生成,勿手改)
├── bin/
│   ├── mihomo               # 当前内核
│   └── mihomo.old           # 升级前的备份(用于回滚)
└── logs/
    └── mihomo.log           # 内核日志(轮转)

/etc/systemd/system/mihomo-cli.service            # 服务单元
/etc/systemd/system/mihomo-cli-sub.timer/.service # 订阅自动更新
/etc/systemd/system/mihomo-cli-auto.timer/.service# 节点自动择优 (node auto)
/usr/local/bin/mihomo                              # cli 本体
```

---

## 5. CLI 参数设计

命令名为 `mihomo-cli`（与内核 `mihomo` 明确区分；内核位于 `~/.config/mihomo-cli/bin/`，不进入 PATH，无命名歧义）。

### 5.1 全局 flags

| flag | 说明 |
|---|---|
| `--json` | 以 JSON 输出（供脚本使用，规划中） |

### 5.2 子命令一览

三层结构: sub(订阅) → group(分组) → node(节点), 全部支持 `#id` 索引别名与 `use/unuse`。
`sub/group/node` 直接回车等价于各自的 `list`。

```
mihomo-cli install [--proxy URL] [--sub URL] [--allow-lan]
                                      # 安装: 下载内核 + systemd 服务 + shell 补全
mihomo-cli uninstall [--purge]        # 卸载; --purge 同时删除 /etc/mihomo-cli
mihomo-cli init                       # 交互式添加第一个订阅
mihomo-cli update                     # 自更新 CLI (别名 upgrade)

mihomo-cli start|stop|restart|status  # 服务生命周期 (status 含链路/定时任务信息)
mihomo-cli run                        # 前台运行(调试)

mihomo-cli sub [= list]               # 订阅列表
mihomo-cli sub add <url> -n <name>    # 添加订阅 (打印当前链路)
mihomo-cli sub rm|update [name|#id|all]
mihomo-cli sub use <name|#id>         # 切换生效订阅 (级联重置 group/node 上下文)
mihomo-cli sub unuse                  # 悬空: 停止服务(网络不再被代理), 级联清空

mihomo-cli group [= list]             # 分组列表 (#1..#n)
mihomo-cli group use <id|名称>        # 设置当前操作分组
mihomo-cli group unuse                # 悬空: 该分组切 DIRECT 直连

mihomo-cli node [= list] [-g group]   # 当前分组的节点列表 (#1..#n)
mihomo-cli node use <id|名称>         # 切换节点 (自动择优开启时黄色提示)
mihomo-cli node unuse                 # 悬空: 分组切 DIRECT 直连
mihomo-cli node test                  # 彩色延迟测试 (绿<200 蓝<500 黄<3000 红, 灰=超时)
mihomo-cli node auto                  # 对当前分组择优 (定时任务复用; 无当前分组则跳过)

mihomo-cli proxy on/off               # 开/关当前 shell 代理 (eval $(mihomo-cli proxy on))
mihomo-cli conn [--watch]             # 活动连接
mihomo-cli traffic                    # 实时流量
mihomo-cli log [-f]                   # 内核日志

mihomo-cli core version|upgrade|rollback
mihomo-cli get/set [key]              # 查看/修改设置 (set 支持热生效)
mihomo-cli doctor                     # 体检
mihomo-cli version                    # cli 版本
```

### 5.3 退出码约定

| 码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 一般错误 |
| 2 | 参数错误 |
| 3 | 服务未运行 |
| 4 | 网络错误(下载失败等) |

---

## 6. 功能设计要点

### 6.1 安装器 (`install`)

1. 检测 arch (`uname -m`: x86_64→amd64, aarch64→arm64；老旧 CPU 提示用 `-compatible` 资产) 与 glibc
2. 从 `https://github.com/MetaCubeX/mihomo/releases/latest` 下载 `mihomo-linux-<arch>-<ver>.gz`（支持 `--proxy` 走代理下载）
3. 解压 → chmod → 安装到 `/etc/mihomo-cli/bin/mihomo`
4. 生成 systemd unit（含 `Restart=on-failure`、日志限制）
5. 若无订阅则引导 `mihomo init`
6. `doctor` 自检并输出摘要

卸载：stop 服务 → disable → 删除 unit/timer/补全、`/usr/local/bin/mihomo-cli`；`--purge` 再删 `/etc/mihomo-cli`。

### 6.2 订阅管理

- 下载时带 `User-Agent: clash.meta`（部分机场按 UA 返回格式）
- systemd timer 定时 `sub update` 并热重载（`PUT /configs`）
- 记录每个 profile 的节点数、流量剩余（解析 `subscription-userinfo` 响应头，如机场提供）
- `sub add/update/use` 后打印当前 sub→group→node 链路并提示切换

### 6.3 三层 use/unuse 语义

- `sub use` 切换生效订阅, 级联重置 group/node 上下文
- `sub unuse` 停止代理服务(网络不再被代理), 级联悬空 group/node
- `group unuse` / `node unuse` 将该分组选择切换为 DIRECT(流量直连), 悬空更低层上下文
- group/node 命令在 sub 悬空时不可用(服务未运行)

### 6.4 自动化（systemd timer 实现，`mihomo-cli set` 动态调整）

| 设置项 | 默认 | 说明 |
|---|---|---|
| `sub-auto-update-enabled` | `true` | 订阅定时自动更新开关 |
| `sub-auto-update-interval` | `24h` | 订阅更新周期 |
| `proxy-auto-select-enabled` | `false` | 定时对当前分组自动择优 |
| `proxy-auto-select-interval` | `30m` | 自动择优周期 |
| `test-url` / `test-timeout` | gstatic 204 / 5000ms | 测速参数 |

择优算法（`node auto`, 定时任务复用）：仅作用于**当前 use 的分组**；
在该分组的**真实节点**（排除子分组与 DIRECT/REJECT 类）中选延迟最低者切换；
未设置当前分组时跳过。手动 `node use` 时若自动择优开启, 输出黄色提示「可能被覆盖」。

### 6.5 配置合并与局域网监听

最终 `runtime/config.yaml` = 订阅 yaml 经如下强制覆盖：

```yaml
external-controller: 127.0.0.1:9090
secret: <随机生成, 存于 config.toml>
log-level: info
mixed-port: <config.toml 中的 mixed_port, 默认 7890>
mode: <proxy-mode 设置: rule/global/direct; 空=跟随订阅; set 时经 PATCH /configs 热切换>
allow-lan / bind-address: set allow-lan true 时为 true / "*" (监听 0.0.0.0, 供局域网设备)
```

overrides.yaml 中的任意顶层键（如 `rules`、`dns`、`tun`）按深度合并覆盖订阅配置。

### 6.6 API 客户端

使用 mihomo external-controller 的端点：

| 用途 | 端点 |
|---|---|
| 代理组/节点 | `GET /proxies` |
| 切换节点 | `PUT /proxies/{group}` body `{"name":"节点"}` |
| 延迟测试 | `GET /proxies/{name}/delay?url=...&timeout=5000` |
| 连接 | `GET /connections` (CLI 轮询实现 watch) |
| 流量 | `GET /traffic` (CLI 聚合展示) |
| 热重载 | `PUT /configs?force=true` |

---

## 7. 安装 / 卸载方式

### 7.1 安装

```bash
# 方式一: 一键脚本
curl -fsSL https://raw.githubusercontent.com/<user>/mihomo-cli/main/install.sh | bash

# 方式二: 手动下载 release
curl -LO https://github.com/<user>/mihomo-cli/releases/latest/download/mihomo-cli-linux-amd64.tar.gz
tar xzf ... && sudo ./mihomo-cli install

# 方式三: go install
go install github.com/<user>/mihomo-cli/cmd/mihomo@latest
```

### 7.2 卸载

```bash
mihomo-cli uninstall          # 保留配置
mihomo-cli uninstall --purge  # 彻底清除
```

### 7.3 典型首次使用流程

```bash
mihomo install
mihomo-cli init                # 粘贴订阅链接
mihomo-cli start && mihomo-cli doctor
eval $(mihomo-cli proxy on)         # 当前 shell 开启代理
```

---

## 8. 技术选型

| 组件 | 选择 | 理由 |
|---|---|---|
| 语言 | Go 1.22+ | 单二进制、交叉编译、与内核同生态 |
| CLI 框架 | spf13/cobra | 事实标准，自动补全/帮助 |
| 配置解析 | gopkg.in/yaml.v3 | clash 生态即 yaml；cli 自身配置用 toml (BurntSushi/toml) |
| HTTP | net/http | 标准库足够 |
| 单元测试 | testing + stretchr/testify | 常规 |

## 9. 开发里程碑

- [ ] M1 骨架: cobra 命令树 + 配置目录 + doctor
- [ ] M2 内核管理: install/core upgrade/rollback + systemd
- [ ] M3 订阅: sub add/update/use + 配置合并 + 定时器
- [ ] M4 控制: proxy/conn/traffic/log (API 客户端)
- [ ] M5 体验: env、install.sh、自更新、shell 补全

## 10. 安全注意

- external-controller 只监听 127.0.0.1 + 随机 secret
- 升级下载校验 release 资产的 sha256（如官方提供）
- systemd 服务默认以非 root 运行，仅启用 TUN 时提示需要 CAP_NET_ADMIN
