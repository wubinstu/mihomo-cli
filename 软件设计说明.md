# mihomo-cli 软件设计说明

> 一个面向 Linux 服务器的纯命令行 Clash 代理工具 —— mihomo 内核的 CLI 外壳
>
> 版本: v1.3 / 日期: 2026-09-26

---

## 1. 项目背景与目标

### 1.1 背景

GitHub 上的 Clash 生态客户端（Clash Verge Rev、FlClash、ClashN 等）几乎全是 GUI 软件，其底层都调用
**mihomo**（原 Clash.Meta）内核。而 Linux 服务器没有图形环境，GUI 客户端无法使用；手动管理 mihomo
（下载内核、写 systemd 单元、更新订阅、切换节点）又非常繁琐。

### 1.2 目标

`mihomo-cli` 是一个 **单二进制、零依赖、纯 CLI** 的 mihomo 内核管理外壳，提供：

- 内核的自动下载、升级、回滚（平台/风味自动探测，本地版本栈离线回滚）
- 订阅的下载、定时更新、多订阅管理
- 代理的启停（systemd 托管，无 GUI）
- 节点/代理组的查看与切换
- 连接、流量、日志的实时查看
- 一键配置系统代理环境变量（`http_proxy` 等）
- **内核全部常用配置项的读写**（一个注册表 + 点号路径，不给每个功能造子命令）
- 安装/卸载一体化，且严格对称

### 1.3 非目标

- 不做 GUI / TUI
- 不重新实现代理内核，只做 mihomo 的包装与编排
- 不支持 Windows / macOS（设计上留有余地，但优先保证 Linux 体验）

---

## 2. 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│                        用户 (SSH 终端)                        │
└───────────────────────────┬──────────────────────────────────┘
                            │ mihomo-cli <subcommand> [flags]
┌───────────────────────────▼──────────────────────────────────┐
│                     mihomo-cli (单二进制)                     │
│  ┌──────────┐  ┌───────────┐  ┌──────────────┐  ┌─────────┐ │
│  │ CLI 层   │  │ 编排层     │  │ 配置注册表    │  │ API 客户端│ │
│  │ cobra    │  │ cmd/…     │  │ internal/cfg │  │ (external│ │
│  └────┬─────┘  └─────┬─────┘  └──────┬───────┘  │ -controller)│
│       └──────────────┴───────────────┴──────────┴─────┬──────┘
│  ┌───────────────────────────────────────────────────▼──────┐ │
│  │ 基础库层: app(路径/配置) core(内核/资源) render(合成)      │ │
│  │            sysd(systemd) subs(订阅) geo(地区) ui(表格)     │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────┬──────────────────┬───────────────────────┬────────────┘
        │ 文件              │ HTTP                  │ 进程托管
┌───────▼──────┐  ┌────────▼─────────┐  ┌──────────▼──────────┐
│ /etc/        │  │ 订阅 URL          │  │ mihomo 内核 (systemd)│
│ mihomo-cli/  │  │ GitHub Releases   │  │ + 3 个 timer        │
└──────────────┘  └──────────────────┘  └─────────────────────┘
```

### 2.1 分层说明

| 层 | 职责 |
|---|---|
| **CLI 层** | spf13/cobra 子命令树，参数解析、帮助、shell 补全 |
| **编排层** | `internal/cmd`：业务流程（安装/订阅/切换/体检），只做"取数→调库→打印" |
| **配置注册表** | `internal/cfg`：一个 key 一条记录，驱动 get/set/补全/校验/同步（本次重构核心） |
| **API 客户端** | mihomo external-controller RESTful API（查询/切换节点、连接、流量、热重载） |
| **内核管理** | `internal/core`：资产规格解析、候选链下载、版本栈 |
| **订阅管理** | `internal/subs`：下载（clash yaml / base64）、更新、配额 |
| **配置合成** | `internal/render`：config.toml + 订阅 + 点号路径段 → runtime/config.yaml |
| **systemd** | `internal/sysd`：service/timer 单元生成、启停、能力申请 |

### 2.2 关键设计决策

1. **cli 不 fork 内核源码**：子进程 + RESTful API 控制，内核可独立升级。
2. **`config.toml` 是唯一权威**：不再有第二份"谁说了算"的配置。render 时对 CLI 管理的键**始终写显式值**
   （配置里没有就写默认值），对点号路径段"没用过就完全不写"。
3. **一个注册表驱动一切**：v1.2 及更早，同一个配置项要在 `configValue()/configDefaults/configKeys/
   set switch/configValueComp` 五处平行维护（`socks-port` 就是漏改出过编译错，补全也只覆盖 6/22 键）。
   v1.3 收敛到 `internal/cfg` 一张表，get/set/补全/`-h`/reset/校验全部推导。
4. **点号路径覆盖内核全部配置项**：不给每个功能造子命令，`config set tun.enable true` 即可。
5. **单二进制**：Go 编写，静态编译，scp 到任何 Linux 服务器即可用。
6. **systemd 托管为默认**，服务器重启自动拉起；`mihomo-cli run` 前台模式用于调试。
7. **权限回归系统惯例**：不把 `/etc/mihomo-cli` 开成"其他人可写"；变更类命令自动提权。

---

## 3. 工作模式

### 3.1 服务模式（默认）

```
mihomo-cli start  →  systemctl start mihomo-cli  →  mihomo 内核 (root + CAP_NET_ADMIN 当开了 TUN)
```

- 内核配置文件由 cli 合成并注入 `external-controller: 127.0.0.1:9090` + 随机 secret
- cli 的所有查询/切换操作都走该 API
- 三个 timer：`mihomo-cli-sub.timer`（订阅更新）、`mihomo-cli-auto.timer`（节点择优）、
  `mihomo-cli-resource.timer`（geo 资源更新）

### 3.2 前台模式（调试）

`mihomo-cli run` 直接 exec 内核，日志输出到 stdout，Ctrl-C 退出。

### 3.3 环境变量模式（按需开代理）

`eval $(mihomo-cli proxy on)` 注入 `http_proxy/https_proxy/all_proxy/no_proxy`；`proxy off` 反注入。

---

## 4. 目录规划（系统级）

服务端(内核)/客户端(CLI)/配置全机共享。变更类命令需 root（自动提权），只读命令全用户可用。

```
/etc/mihomo-cli/
├── config.toml              # 唯一权威 (0640); 文件头有管理警告
├── config.toml.pre-1.3.bak  # 旧格式迁移前的自动备份 (只做一次)
├── bin/
│   ├── mihomo               # 当前内核 (0755)
│   └── versions/<ver>/      # 本地版本栈 (rollback 出栈, 最多 3 个)
├── profiles/
│   └── <name>.yaml          # 各订阅的原始下载文件
├── runtime/
│   ├── config.yaml          # 合成后给内核的实际配置(自动生成,勿手改)
│   ├── geoip.metadb / GeoSite.dat / GeoLite2-ASN.mmdb / geoip.dat
│   ├── cache.db             # 内核缓存(fqdn/进程名)
│   ├── connseq.json         # top 连接编号
│   └── node-region.json     # 节点地区缓存
└── logs/                    # 已停用 (日志交给 journald)

/etc/systemd/system/mihomo-cli.service            # StandardOutput=journal
/etc/systemd/system/mihomo-cli-{sub,auto,resource}.{service,timer}
/usr/bin/mihomo-cli                                # cli 本体
```

---

## 5. CLI 参数设计

命令名为 `mihomo-cli`（与内核 `mihomo` 明确区分；内核位于 `/etc/mihomo-cli/bin/`，不进 PATH）。

### 5.1 全局 flags

| flag | 说明 |
|---|---|
| `-h, --help` | 帮助；子命令裸执行同样给帮助，`<key> -h` 给单键详情 |

### 5.2 三级权限模型

| 类别 | 命令 | 权限 |
|---|---|---|
| 只读 | `config get` `status` `doctor` `top` `ping` `node list/test` `group list` `sub list` `rule list` `dns list` `tun list` `log` `resource core version/history` `proxy on/off` | 全用户 |
| 变更 | `install` `uninstall` `start/stop/restart` `sub add/rm/rename/update/use/unuse` `group use/unuse` `node use/unuse/auto` `rule add/rm/enable/disable` `dns use/unuse/on/off` `tun on/off` `config set/reset-default/unset/update-*` `resource */update` `resource core upgrade/rollback` | root（自动提权） |

自动提权规则：非 root + stdin 是 TTY → `exec sudo` 原样重跑（一次密码）；
非 TTY（脚本/管道/ssh 非交互）→ 立即报错并给出确切命令 `sudo mihomo-cli …`，不悬挂等待。

### 5.3 子命令一览

```
mihomo-cli install [--core <spec>] [--arch <name>] [--resource mmdb|asn|geoip|geosite|all]
                   [--systemd] [--completion bash|zsh|fish] [--proxy URL] [--mirror URL]
mihomo-cli uninstall [--core] [--resource] [--systemd] [--completion [shell]] [--purge]
mihomo-cli update                      # 自更新 CLI (别名 upgrade)
mihomo-cli run                         # 前台运行(调试)

mihomo-cli start|stop|restart|status
mihomo-cli sub [= list]                # add <name> <url> | rm | rename | update | use | unuse
mihomo-cli group [= list]              # use | unuse
mihomo-cli node [= list] [-g group]    # use | unuse | test | auto
mihomo-cli proxy on|off
mihomo-cli rule list|add|enable|disable|rm
mihomo-cli dns  on|off|use|unuse|list
mihomo-cli tun  on|off|list
mihomo-cli top [watch N] [kill <id..>]
mihomo-cli ping [site...]
mihomo-cli config get [key|section] | set <key> <value> | reset-default [key]
                  unset <key...> | update-file [key...] | update-service [key...]
mihomo-cli resource core version|upgrade [<spec>]|rollback|history
                  mmdb|asn|geoip|geosite info|update | update-all
mihomo-cli log [-f]     doctor     version     completion
```

### 5.4 退出码约定

| 码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 一般错误 |
| 2 | 参数错误（cobra） |
| 3 | 服务未运行 |
| 4 | 网络错误(下载失败等) |

---

## 6. 功能设计要点

### 6.1 配置注册表（`internal/cfg`）

```go
type Kind int  // KindBool KindTri KindPort KindEnum KindDur KindInt KindText
               // KindURL KindMirror KindNameList KindCIDRList KindList KindState

type Key struct {
    Name    string   // "mixed-port" / "tun.enable"
    Section string   // core | cli | timer | dns | tun
    Kind    Kind
    Def     string   // 默认值
    Enum    []string
    Comp    []string // 显式补全候选
    Usage   string   // 中文说明(en 由 i18n 查表)
    Min, Max int
    Restart bool     // 改动是否需要重建监听
    Get, Set func(*app.Settings) (string, error)
}
var Keys = []Key{ … }
```

推导出的产物：

| 产出 | 位置 |
|---|---|
| `config get` 五列表 | `cmd/config.go` 只负责渲染 |
| `config set` 校验/报错 | `Key.Parse` |
| TAB 补全（KEY 与 VALUE） | `CompleteKeys` / `CompleteValues` |
| `config set <key> -h` | 由 Usage/Enum/Def/Restart 拼 |
| `reset-default` | 由 `Def` 驱动 |
| `doctor` 的配置体检 | L1 全量校验 |

**两类键**

- **注册键**：扁平键存 `Settings` 字段；点号路径键存 `Settings.Overrides`。
- **通用键**（未注册）：`config set <任意yaml路径> <值>` 直接写进 `[overrides]`，值按字面推断类型
  （`true/false`→bool、整数→int64、含逗号→列表、其余字符串）。

**Managed 语义**（关键设计）

| | 空值的含义 | render 行为 |
|---|---|---|
| 扁平键（core/cli/timer） | 用默认值 | 始终写显式值（CLI 权威） |
| 点号路径段（dns/tun） | 未接管 | 完全不写，订阅/内核原样保留 |

三态键的第三个值 `sub` = 显式"跟随订阅"，与空值语义完全不同、可补全、可在 `-h` 里看到。

### 6.2 两级配置值校验

| 级 | 时机 | 策略 |
|---|---|---|
| L1 形态 | `config set` 写盘前 | 由 Kind 决定（bool/int/enum/端口/时长/URL/IP/CIDR）。**不合法直接拒绝写入**，报错四元组：键名/填入值/期望/合法值 |
| L2 语义 | 真正使用该值时 | 本地无法判断的（URL 404、镜像站不通、内核拒绝的组合）。接受写入，使用点把错误包成 `config <key> = <value>: <原因>` |

`doctor` 的 `Config values` 行对 config.toml 全量跑 L1，覆盖手改文件的情况并点名第一个不合法的键。

### 6.3 安装器（`install`）与卸载器（`uninstall`）

严格对称，各开关只动自己那一部分：

| install | uninstall |
|---|---|
| `--core <spec>` | `--core`（先 stop 服务，删内核 + 版本栈） |
| `--resource <…>` | `--resource`（删 4 个 geo 文件） |
| `--systemd` | `--systemd`（disable + 删单元 + daemon-reload） |
| `--completion <shell>` | `--completion [shell…]`（不带值=全删） |
| — | `--purge`（全部 + `/etc/mihomo-cli` + 二进制自身） |

- 幂等：文件不存在则下载创建，存在则更新刷新；无参数 = 帮助
- 结束检查：内核 + systemd 就绪而服务未运行 → 以最小配置自动拉起服务
- 权限：`chmod -R o=rwX` 已移除（v1.2 及更早的做法）；`app.HardenPerms()` 一次性纠正历史残留

### 6.4 内核安装包规格与版本栈

资产名 `mihomo-linux-<arch>[-<flavor>]-<version>.gz`，其中**版本轴与风味轴正交**：

```
版本轴: v1.19.31
风味轴: v1/v2/v3 (GOAMD64 微架构档位) | go120/go123 (Go 工具链/最低 glibc)
        compatible (旧 CPU + 旧 glibc, 仅 amd64)
平台  : linux/<arch> —— 自动探测并记住在 config.toml (core_platform/core_flavor/core_version)
```

CLI 只暴露两个正交轴的简写：`--core auto|latest|compatible|v1|v2|v3|go120|go123|<flavor>[-<combo>][:<version>]`

```
--core auto                 自动探测风味 + 最新版
--core v1.19.31             指定版本
--core compatible:v1.19.19  风味 + 版本
```

**自动探测 = 候选链 + 试跑校验**：
`plain → v3 → v2 → v1 → compatible → *-go120`（arm64/armv7 只有 plain），每个候选下载后跑一次 `<bin> -v`，
跑不起来（缺指令集/glibc 太低会 exec 失败）就降级下一个。

**版本栈**：`bin/versions/<ver>/mihomo` 保存最近 3 个版本，`config.toml` 的 `[[core_history]]` 记录栈。
`install --core` / `resource core upgrade` 装新前把当前版本入栈；`resource core rollback` 出栈装回
（**纯本地，不联网** —— 新内核把代理链路搞坏时，网络可能正走在坏掉的内核上，离线才能可靠回退）；
`resource core history` 看栈。

### 6.5 `resource core upgrade` 的语义

`upgrade [<spec>]` 是**广义的"切换版本"**：接受任意版本号（哪怕比当前旧），只要规格与当前不同就换。
省略参数 = 沿用已记住的平台与风味 + 最新版。因此降级不需要另一个入口，也不与 rollback 语义重叠。

### 6.6 配置合成（`render`）

`runtime/config.yaml` = 当前订阅 yaml ← `[overrides]` 点号路径段（深合并）← CLI 运行时注入层（强制）

```yaml
external-controller: 127.0.0.1:9090     # 强制
secret: <随机生成, 存于 config.toml>     # 强制
mixed-port: 7890                        # 具体端口才写
socks-port / port: 7891 / 7892          # off/sub 一律不写
redir-port: <删除>                      # 避免订阅遗留的 tproxy/redir 冲突
allow-lan / bind-address                # allow-lan 为 true 时 bind-address: "*"
mode / log-level                        # 三态键, "sub" 不写
tcp-concurrent / unified-delay / keep-alive-interval
rules: <用户规则(enabled)> + <订阅规则>  # 用户规则在前, 首条匹配即生效
dns: <dns 段>                           # 自定义 nameserver 时; default-nameserver 必须纯 IP
tun: <tun 段>                           # 用过才写
geox-url: <镜像>                        # 订阅无该键且含 rules 时注入
```

无生效订阅时生成最小配置（`mode: direct`），服务保持运行，全部流量直连 —— 这是**任何 unuse 都不停服务**的基础。

### 6.7 三层 use/unuse 语义

- `sub use` 切换生效订阅，级联重置 group/node 上下文
- `sub unuse` 内核热重载为空配置(全部 DIRECT)，级联悬空 group/node；`sub rm` 删到最后一个同样处理
- `group unuse` / `node unuse` 将该分组选择切换为 DIRECT(流量直连)，悬空更低层上下文
- group/node 命令在 sub 悬空时提示「无订阅」(服务在运行, API 正常)

### 6.8 用户规则（`rule`）

结构化存储 (type/condition/strategy/no_resolve/enabled)，独立于订阅；渲染时置于订阅规则之前。
`rule add --type/--condition/--strategy/--no-resolve`（31 种类型上下文帮助+补全）；
语法经内核 reload 校验，失败自动回滚。`enable/disable` 独立启停，`list` 支持 `--sub/--type/--condition/--strategy/--no-resolve` 过滤。

### 6.9 自动化（systemd timer，`config set` 动态调整）

| 设置项 | 默认 | 说明 |
|---|---|---|
| `sub-auto-update-enabled` / `-interval` | true / 24h | 订阅定时自动更新 |
| `node-auto-select-enabled` / `-interval` | false / 30m | 定时对当前分组自动择优 |
| `resource-auto-update-enabled` / `-interval` | false / 24h | geo 资源定时更新 |
| `test-url` / `test-timeout` | gstatic-204 / 5000ms | 测速参数 |

择优算法（`node auto`，定时任务复用）：仅作用于**当前 use 的分组**；在该分组的**真实节点**
（排除子分组与 DIRECT/REJECT 类）中选延迟最低者切换；未设置当前分组时跳过。

### 6.10 API 客户端

| 用途 | 端点 |
|---|---|
| 代理组/节点 | `GET /proxies` |
| 切换节点 | `PUT /proxies/{group}` |
| 延迟测试 | `GET /group/{name}/delay?url=…&timeout=…` |
| 连接 | `GET /connections`（CLI 轮询实现 watch） |
| 流量 | `GET /traffic` |
| 读运行配置 | `GET /configs`（`config get` 的 RUNNING 列、`update-file` 的来源） |
| 局部热切换 | `PATCH /configs`（mode / log-level） |
| 热重载 | `PUT /configs?force=true` |

改配置后的生效策略：`proxy-mode`/`log-level` → PATCH；端口/TUN → 先试热重载并校验运行态，
跟不上才 `systemctl restart`（**避免"上一个实例还没释放端口"导致的 fatal 崩溃循环**）。

---

## 7. TUN / DNS 设计

### 7.1 为什么不造 `tun`/`dns` 的完整子命令树

内核配置项有几百个。为每个功能造一个子命令（tun/dns/sniffer/hosts/ntp/script…）必然爆炸，
而且每个子命令都要重复一遍 list/use/unuse/补全/校验 —— 这正是历史上反复出 bug 的模式。
因此只保留 `on/off/list` 三个动作作为糖，其余全部走 `config` 的点号路径。

### 7.2 TUN 安全护栏

`tun on`（=`config set tun.enable true`）在任何情况下都要先过三道关，任一不过就拒绝并解释原因：

1. `/dev/net/tun` 不存在 → 容器/内核未启用 TUN，提示改用 mixed-port；
2. 自动把 `AmbientCapabilities=CAP_NET_ADMIN CAP_NET_RAW` 写进 systemd 单元；
3. **SSH 自保**：`auto-route` 会把默认路由指进 tun，SSH 会话极易断线。
   从 `SSH_CONNECTION`/`SSH_CLIENT` 取对端 IP，取其所在网段，要求它已在
   `tun.route-exclude-address` 里；不在则给出确切的 `config set tun.route-exclude-address <对端网段>,…`
   命令，让用户确认后再开。

### 7.3 DNS 段

`dns.use`（预设/`sub#`/裸 IP）就是 `config set dns.nameserver …` 的糖。
其余键（`enable`/`listen`/`enhanced-mode`/`fake-ip*`/`default-nameserver`/`fallback`/
`use-system-hosts`/`ipv6`）全部走点号路径，见 `config get dns` 的默认值表。
DoH/DoT 的 `default-nameserver` 必须纯 IP（内核要求），render 时用内置 223.5.5.5/119.29.29.29 兜底。

---

## 8. 安装 / 卸载方式

```bash
curl -fsSL https://raw.githubusercontent.com/wubinstu/mihomo-cli/main/install.sh | sudo bash
curl -LO https://github.com/wubinstu/mihomo-cli/releases/latest/download/mihomo-cli_linux_amd64.tar.gz
sudo mihomo-cli update                     # 自更新
sudo mihomo-cli uninstall --purge          # 彻底卸干净
```

---

## 9. 技术选型

| 组件 | 选择 | 理由 |
|---|---|---|
| 语言 | Go 1.22+ | 单二进制、交叉编译、与内核同生态 |
| CLI 框架 | spf13/cobra | 事实标准，自动补全/帮助 |
| 配置解析 | BurntSushi/toml（cli 自身）/ gopkg.in/yaml.v3（内核配置） | yaml 是 clash 生态；toml 适合给人看的表格式 |
| HTTP | net/http | 标准库足够 |
| 单元测试 | testing + 表驱动 | cfg/render/ui/i18n 纯函数优先 |

## 10. 质量保障

- `go test ./...`：注册表单测（Kind 全覆盖、默认值合法、补全候选合法、端口别名、段顺序）+
  render 单测（默认值必须写入、`sub` 不写、端口键名映射、用户规则前置、悬空最小配置）+
  ui 单测（CJK 宽度、ANSI 不计宽、跨段对齐）+
  **i18n 静态审计**（`T()` 的 key 必须在 en 表里、无裸中文、注册表 Usage 必须有翻译）
- `scripts/completion-check.sh`：57 项补全断言（禁止文件补全 + 每个键都有值补全）
- 回归清单见交接文档第 6 章

## 11. 安全注意

- external-controller 只监听 127.0.0.1 + 随机 secret
- `/etc/mihomo-cli` 权限回归系统惯例（root:root，目录 755 / 文件 644），变更类命令自动提权
- 升级下载走 指定代理 → 自身代理 → 镜像 → 直连 的链；内核安装后必须 `<bin> -v` 试跑通过
- TUN 开启前强制 SSH 对端网段排除（见 §7.2）
- 敏感信息（服务器地址/订阅 token）严禁入库
